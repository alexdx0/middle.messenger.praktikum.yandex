<main class="main-layout">
  <div class="left-side">
    {{!-- {{{ Link title="Профиль >" className="link-to-profile" to="/settings" }}}
    {{{ SearchInput name="search" }}}
    {{{ ChatsList chats=chats }}} --}}
  </div>
  <div class="right-side">
    <div class="contact-info"></div>
    {{!-- {{{ MessagesList }}} --}}
    <div class="messaging"></div>
    <div>
      {{!-- {{chats}} --}}
    </div>
    {{{ ArrowButton direction="right" onClick=test}}}
  </div>
</main>
